// Dentro dos objetos podemos ter funções que são chamadas de metodos, que são extremamente uteis já que tem acesso as chaves do objeto.

// Podem ser usadas Factory function e Constructor function para criar varios objetos que contenham as mesmas chaves.

// A Constructor function utiliza a chave (this.) para fazer referencia ao objeto que será criado, como serão objetos com nomes diferentes não poderias usar um nome unico para criar as chaves da função.

// Para travar uma Constructor function pode ser usado o 'Object.freeze(this)' para que nada possa altera-ló.